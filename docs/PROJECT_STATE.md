# CLASSIFIRE Project State

**Verified snapshot:** 2026-09-06 AEST. **Health:** usable bounded Draft prototype;
not a production-ready platform. Accepted ADRs 0001/0002 remain unchanged: independent
capabilities, shared deterministic services, optional AI, conditional OpenClaw retirement
only after protection parity. The database/retained storage govern live state;
portable packages do not confer approval, locks or release authority.

**Verified shared baseline:** `b6792f998ae73c6755a84cd4f1f8a24afa6dc00f`,
[merged PR #196](https://github.com/Slayde91/classifire/pull/196).
Exact-head CI [33988763234](https://github.com/Slayde91/classifire/actions/runs/33988763234)
passed 1,396 tests; [main CI 33989222878](https://github.com/Slayde91/classifire/actions/runs/33989222878)
is confirmed successful. Pricing XLSX selection and the supplied logo are merged.
**Current increment:** `feat/draft-system-reports-20260906`, isolated worktree
`C:\CLASSIFIRE\.tmp\draft-system-reports-20260906`, based on that main.
This is a prepublication checkpoint; current Git/PR/CI outrank it.

## Evidence-based implementation snapshot

| Area | Implemented and evidenced | Remaining gap |
| --- | --- | --- |
| Scope | Manual editor, immutable JSON/import, graph validation, PDF page-linked observations; prior real-scanner/browser/restart evidence | Word/XLSX/images/drawings intake UI, automatic analysis, instances, full planes/treatments and contradictions |
| Technical | Source-bound retrieval, keep/reject/history; merged pinned-field publication and saved substrate/gap numeric review | Partial checks are not compatibility; material/size/FRL/insulation/configuration and complete applicability remain unassessed |
| Estimating | Manual rates, unknown quantities, original/override history, omit/restore; merged XLSX upload/scan/mapping/explicit-rate selection UI with exact cells | Governed defaults/inference, broader components, full recovery, more formats/units/currencies |
| Reporting | Independent Scope/Estimate reports plus current scope-and-system PDF+XLSX frozen snapshots; workbook provenance | Combined profile, complete package export |
| UI/integration | FastAPI/Jinja calls shared services; attached logo on sign-in/sidebar/new Draft reports | ChatGPT adapter, full tenant/privacy and operational assurance |
| Packages | Scope v1/v2/v3 exchange, match/Estimate JSON, retained reports; current Estimate v2 freezes workbook selections | Full ProjectPackage ZIP, source/history export rights and other imports |
| Operations | Canonical authority guards, admissions/locks, human release, exact-byte storage and transport/journal foundations | Production Phase 8-14 exits and OpenClaw protection parity are unproven |

## Merged pricing increment and verification (PR #196)

Shared source intake reuses existing PDF/ClamAV/quarantine controls. Additive migration
0033 retains old Estimate/report bytes and exact StoredFile binding. A fixed bounded
XLSX worker preserves cell kinds, formulas and unknowns; it never evaluates formulas.
Explicit column mapping and exact-unit selection append an Estimate v2 override with
source hashes/cells and a required recovery explanation. No canonical library activation
or technical approval occurs. Existing manual v1/history and PDF entry points remain.
See [the supported contract and demo](./DRAFT_PRICING_XLSX.md).

- Targeted pricing/service/HTTP/deployment tests: **23 passed, 1 existing Starlette warning**
  (19.43 seconds), before the additional archive/tampering regression cases.
- Additive migration test passed, including old output preservation and downgrade refusal.
- Real Chrome + real ClamD: upload, scan, map, select Rates!D5 = 120.25 AUD,
  preserve original 100, then reasoned override 125. Blank/formula rates unselectable.
- Actual restart preserved exact Estimate r2/r3/r4 and both report downloads.
  No browser page errors. Four PDF pages inspected: readable and correctly branded.
- Excel output independently reopened: source cells/types/values present and safe literal
  provenance. Artifact-tool renders were produced but its process exited 1 without a
  diagnostic; its preview also misrenders empty strings. This is a preview-tool limitation,
  not evidence of empty-cell values in the independently inspected workbook.
- Exact served logo SHA-256 matches the user PNG:
  `fa738653f44b4bd148de81c6190b7aed572c036e8589f18540b9cdaf02fdb46a`.
  Sign-in/sidebar screenshots inspected; old demos on 8803/8804 also serve exact bytes.
- Mypy passed 175 source files; Bandit passed; one Alembic head is 0033.
  Final regression/static/documentation results are recorded before publication below.
- No customer/operational database, provider, canonical release or production audit ran.

## Current report increment and active gaps

The existing report table/service now accepts an exact saved System Match and its
embedded Scope; no Estimate, retrieval or AI is required. UI preview/create and
historical PDF/XLSX use one immutable snapshot. Candidate decisions and partial
measurements stay unapproved, missing criteria stay visible, and source/review changes
mark old reports stale without replacing bytes. Technical read is enforced alongside
project/ownership checks. No migration, dependency or authority change is needed.
See [contract](./DRAFT_SYSTEM_REPORT_CONTRACT.md).

Local work is report service/rendering/UI, tests and aligned documentation only.
Protected root remains unrelated recovery evidence; inspect current status before
any operation. Three untracked ProjectPackage candidate files in
`.tmp/project-package-draft-20260905` and prior worktrees/demos remain preserved.
Runtime data, harnesses, screenshots and generated synthetic files stay local.

Production parser isolation, scanner refresh/availability, quotas/retention, tenancy,
complete technical and recovery rules, report breadth and package exchange remain
open. No operational database, customer evidence, provider, canonical lock or release
was used. No concrete blocker to normal verification/publication is known.

## Recommended Next Actions

1. Finish current scope-and-system report validation/publication if outstanding;
   verify Git/CI first and do not repeat merged work.
2. Add **service-size review** to the current measured-limit UI using existing pinned
   minimum/maximum service-size fields. It is a material missing applicability input;
   another report format is less valuable than a user-testable technical improvement.
   Require explicit measurement/source interpretation, retain unresolved ambiguous
   cases and exact history, and keep numeric outcomes partial/unapproved.
3. Continue broader evidence analysis, full applicability and governed pricing, combined
   reporting, ProjectPackage exchange and ChatGPT access through shared commands.
   Trial supported interactions before broad polish; production gates remain separate.

## Historical pricing local verification checkpoint (PR #196)

Affected Draft/Estimate/PDF/report/Scope UI/migration regression: **194 passed,
2 existing dependency warnings** (515.13 seconds). After final permission-error
classification and pre-save permission recheck, targeted pricing/report/HTTP regression:
**41 passed, 1 existing warning** (59.11 seconds). These overlapping counts are not
additive. Browser/restart receipts and exact historical downloads passed. Independent
synthetic database counts: one pricing source, four Draft Estimate revisions, zero
canonical Estimates/Openings/Services/Physical Model Locks or pricing library records/
releases. Documentation links and whitespace checks passed. Final Ruff, Mypy (175 source files), Bandit and one Alembic head (0033) passed.
PR #196 subsequently passed exact-head CI and merged as recorded above.

## Current report validation checkpoint

- Cross-capability Scope/Estimate reports, candidate and measured-review regression:
  **170 passed, 1 existing Starlette deprecation warning** (421.25 seconds).
- Final Excel heading-height correction: **5 focused report/service/HTTP tests passed**,
  same existing warning (15.15 seconds). These overlap the larger suite, not extra coverage.
- Ruff passed; Mypy passed 176 source files; Bandit passed; migration head remains
  `0033_draft_pricing_sources`. No schema migration was added.
- Real Chrome created a saved review report, changed the review, displayed staleness
  and downloaded identical historical PDF/XLSX. No page errors. An actual subsequent
  process restart preserved the final exact downloads and stale status.
- Nine-page PDF layout inspected; the three new Excel sheets were visually checked.
  A real heading-height issue was corrected. Artifact-tool still exits 1 without
  diagnostics after saving previews; independent openpyxl/ZIP checks passed for
  literal cells, sheet structure, heading heights and exact embedded logo. Native
  Excel rendering was not verified.
- Final synthetic database has three retained report pairs from repeated browser
  checks and **zero Draft Estimates, canonical Estimates, Openings, Services or
  Physical Model Locks**. No matching/estimation is triggered by reporting.
- Served and embedded logo SHA-256 is the user's exact PNG:
  `fa738653f44b4bd148de81c6190b7aed572c036e8589f18540b9cdaf02fdb46a`.
- Documentation links and diff whitespace checked. AGENTS.md/accepted ADRs were
  reviewed and already aligned; no unnecessary instruction or architecture rewrite.

Receipts and generated outputs remain local in `.tmp/system-report-artifacts`.
Exact-head hosted CI/review and verified merge are still required at this checkpoint;
use live Git/PR evidence to determine publication rather than treating this as a merge claim.

Final review also added a stale warning before report creation, using the existing
match/source freshness checks. The focused HTTP journey passed (1 test, existing
Starlette warning, 7.10 seconds); Mypy again passed 176 files. Real Chrome confirmed
the warning and byte-identical existing downloads after loading the final code.
Publication PR: [#197](https://github.com/Slayde91/classifire/pull/197); check its
current head, CI/review and merge state before continuing.
